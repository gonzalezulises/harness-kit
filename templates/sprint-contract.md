# Sprint Contract — {{FEATURE_ID}}

Negotiated **before** work starts. Its job is to make scope disagreements surface now,
when they cost a paragraph, instead of at review, when they cost a session.

## Scope

**In scope — this and nothing else:**

-

**Explicitly out of scope:**

-

**Files expected to change:**

-

Touching a file outside this list is a scope change. Stop, record it in `PROGRESS.md`,
and either renegotiate or defer it.

## Definition of Done

- [ ] Layer 1 (static): `make check` exits 0
- [ ] Layer 2 (runtime): the behavior below actually runs
- [ ] Layer 3 (e2e): exercised the way a real user would
- [ ] Evidence recorded in `feature_list.json`
- [ ] `make clean-check` passes

**Target behavior, in user terms:**

>

## Known risks

-

## Not doing (and why)

Deferrals recorded here instead of discovered later:

-
