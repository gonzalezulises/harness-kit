# Harness 33–37 closure implementation plan

> For agentic workers: use superpowers:subagent-driven-development for bounded implementation and independent review. This plan executes the owner's 2026-09-07 closure instruction without further routine approval pauses.

**Goal:** Integrate, publish and accept increments #33–#37 with a real authenticated product pilot and an isolated distribution consumer.

**Architecture:** Preserve the existing signed runtime, journal and protected judge. Add only missing closed publication behavior and causal corrections; distinguish local functional evidence from real-host acceptance. Keep one implementation task active and a single closure inventory.

**Tech Stack:** Bash 3.2-compatible kit, Node ESM with existing locked yaml/zod, Python standard-library distribution manager, GitHub plugin for repository operations.

**Spec:** The owner's seven-part closure instruction of 2026-09-07, recorded in `docs/implementation/2026-09-07-closure/mandate.md`, plus the existing product, interruption and distribution contracts.

## Global constraints

- Exact adoption authorized: PR34 head `b6b93e4fd8241298471e9cfc4356ecca47412f43`; no material judge-policy change without a specific decision.
- Ordinary required checks and independent reviews must succeed before integration. No bypass, invented signature, or fabricated review.
- Preserve signed resource limits and historical evidence; F09 is historical blocked behavior, not a candidate for artificial promotion.
- Casabat and Aurobalance are evidence only and never mutation targets.
- Real pilot requires authenticated implementer/reviewer, enrolled authority and proved host isolation. A fixture is never real acceptance.
- Repair mechanical problems autonomously within existing authority. Blocked external capability does not prevent other useful work.
- No client application deployment. Publish only the resulting Harness Kit version after applicable gates.

## Task 1: Reconcile and adopt the exact protected judge

Files: closure `inventory.json`, `mandate.md`, and `evidence/judge-review.md`; later update current prose in `docs/protected-judge-adoption.md` without rewriting its historical verification receipt.

- [x] Reconcile exact PR heads and bases, current CI, reviewer threads and active required ruleset through GitHub.
- [x] Verify existing worktree is clean; `./init.sh` exits 0 with 286 passed, 0 failed.
- [x] Obtain read-only independent review of exact PR34 head, including all 30 manifest files/modes and retained evidence links.
- [x] Mark #34 ready; recheck exact head/base and CI; merge through the ordinary API with expected head SHA.
- [x] Read back merged main and record the adoption event and unchanged protection rules.

## Task 2: Correct the remaining product supervisor defects

Modify `packs/autonomy/repo-template/scripts/quality-orchestrator/product.mjs`; create `tests/product-closure.test.mjs` and a test-only helper if needed in the same runtime directory. Preserve all existing test files and historical receipts.

Inputs: existing runtime product.v1/v2 public APIs and the P0-R1-M1/M3 findings in `docs/implementation/2026-09-06-product-loop/evidence/review.json`.
Output: linked source worktrees can prepare a correct local PR commit without source index/ref mutation; invalid verifier UTF-8 yields a controlled failure without terminating the controller.

- [x] Witness a linked-worktree fixture reach HANDOFF_PREPARED, with parent and frozen files preserved; on current source its objects lookup must fail.
- [x] Run the malformed-verifier case in a child controller process: require an ordinary structured failure, zero uncaught exception and zero duplicate verifier effects on resume. Witness RED on current source.
- [x] Resolve Git's common object directory using `rev-parse --path-format=absolute --git-common-dir`; preserve existing realpath, newline and immutable-input checks.
- [x] Catch strict UTF-8 decoding errors inside the verifier close callback and reject its promise, so the existing runtime error boundary can classify the failure.
- [x] Run causal GREEN, affected legacy product tests and syntax checks. Retain minimal RED/GREEN receipts and request a scoped independent review.

## Task 3: Complete bounded remote PR publication

Inspect existing `product.mjs`, `execution.mjs`, `github-actions.mjs`, their schemas, and `classify.mjs` before choosing the smallest compatible interface. Record the exact publication contract in this closure directory before implementation.

- [ ] Bind the target repository, base/head identity and publication content to current accepted authority; never inherit permission for another repository.
- [ ] Add a closed publication operation that creates the remote PR through the authenticated fixed GitHub transport, returns a verified remote identity and persists intent/result for resume.
- [ ] Exercise actual publication orchestration with deterministic transport fixtures, including rejection before effects for stale source/authority and reconciliation after an uncertain response without duplicate POST.
- [ ] Run an authenticated real operation only where the required host, authority and credentials are available. If a prerequisite is absent, record the concrete missing capability and preserve the implementation and exact handoff for an authorized host.

## Task 4: Integrate distribution and close reviews

- [ ] Merge the existing distribution delta locally without replacing product/interruption records; reconcile shared feature, quality and progress files.
- [ ] Account for F26-F1-M3 as an unavailable historical input. Preserve that fact, verify current causal receipts, and do not invent historical bytes.
- [ ] Complete a read-only independent review of all effective changed source and current documentation; preserve the prior interrupted-review receipts separately.
- [ ] Correct findings with causal regression evidence and scoped re-review, retaining all remaining acceptance gaps in the inventory.
- [ ] Run required local verification and refresh affected evidence against frozen final source.
- [ ] Publish verified changes via the GitHub plugin and integrate dependent PRs only after their ordinary current-base checks pass.

## Task 5: Real acceptance and release

- [ ] Demonstrate the authenticated real pilot: RED, functional change, GREEN, independent review, bounded correction where needed, resume and an actual remote PR.
- [ ] Demonstrate installation, upgrade and rollback in a consumer whose exact scope and permissions are recorded; do not use a client repository.
- [ ] Verify the integrated commit, complete acceptance using real enrolled authority, then use the repository's release workflow to publish the resulting version.
- [ ] Deliver exact commit/PR/release identities and observed evidence. If any current requirement remains unmet, keep overall closure INCOMPLETE and report the minimum external action.
