import {registerHooks} from 'node:module';
import {createHash} from 'node:crypto';
registerHooks({load(url,context,nextLoad){const result=nextLoad(url,context);if(!url.endsWith('/quality-orchestrator/product.mjs'))return result;let source=String(result.source);if(createHash('sha256').update(source).digest('hex')!=="acc0e4c8402dde24e9c7d88cf168be7744c0b9a4ccc435ef1282bb087ac0b51a")throw Error('mutation source changed');for(const [before,after] of [["const payload=journal.getObject(n.digest);", "const payload=normalized(s,journal.readAcknowledgement(ackDigest(s,p.key)));"], ["const persisted=journal.getObject(s.normalized[key].digest),", "const persisted=payload,"]]){if(!source.includes(before))throw Error('causal read guard absent');source=source.replace(before,after);}return {...result,source};}});
