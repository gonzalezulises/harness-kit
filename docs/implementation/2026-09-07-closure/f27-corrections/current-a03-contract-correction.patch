--- preserved-history/human-interruption.test.mjs
+++ current/human-interruption.test.mjs
@@ -33,7 +33,7 @@
 
 test('A01 canonical representation and derived golden corpus recover within the original grant',async t=>{const f=setup(t,{canonical:true});const approval=f.wire.approval.signature;await review(f);const s=state(f);assert.equal(s.remediations.length,1);assert.equal(s.remediations[0].classification,'AUTO_REMEDIABLE');assert.equal(f.wire.approval.signature,approval);assert.notEqual(fs.readFileSync(path.join(f.root,'golden.json'),'utf8'),'{}');assert.equal(s.verified_state,'UNKNOWN');});
 test('A02 stale mechanical run binding is replaced and replayed without resetting counts',async t=>{const f=setup(t);await f.runtime.runProduct(f.handle,{maxSteps:1});const before=state(f);f.setBinding('threshold: 100\nenabled: true\nstatus: active\ntitle: hello\n');const after=await review(f);assert.notEqual(after.activeRunId,f.wire.objective.runId);assert.ok(after.counters.starts>before.counters.starts);assert.equal(after.rebindings.length,1);});
-test('A03 credential-shaped infrastructure field is replaced without changing reviewer judgment',async t=>{const f=setup(t,{credential:true});const s=await review(f);assert.equal(s.tooling.failures,1);assert.equal(s.interruption?.classification,'AUTO_REMEDIABLE');assert.equal(s.review.findings[0].severity,'High');assert.equal(s.review.findings[0].description,'Semantic regression is false');assert.match(s.review.findings[0].id,/^finding:[a-f0-9]{64}$/);assertDurable(f,s);});
+test('A03 unqualified credential-shaped infrastructure rejection stays diagnostic without retry',async t=>{const f=setup(t,{credential:true}),signature=f.wire.approval.signature;const s=await f.runtime.runProduct(f.handle,{maxSteps:5});assert.equal(s.status,'OPERATIONAL_BLOCKED');assert.equal(s.interruption.classification,'OPERATIONAL_DIAGNOSIS');assert.equal(s.counters.fullReview,0);assert.equal(s.ingestions.length,0);assert.equal(f.count().filter(c=>c.kind==='REVIEW').length,1);const before=f.runtime.journal.replay(f.ctx).budget.spent;await f.runtime.runProduct(f.handle);assert.equal(f.runtime.journal.replay(f.ctx).budget.spent,before);assert.equal(f.count().filter(c=>c.kind==='REVIEW').length,1);assert.equal(f.wire.approval.signature,signature);});
 test('A04 invalid evidence refs retry within tooling budget then bind only exact source',async t=>{const f=setup(t,{refs:'retry'});const s=await review(f);assert.equal(s.tooling.failures,1);assert.equal(s.counters.fullReview,1);assert.ok(s.ingestions[0].normalizedOutputDigest);assertDurable(f,s);});
 test('A05 invalid durable test id is deterministically generated from approved case',async t=>{const f=setup(t,{badId:true});const s=await review(f),record=s.ingestions[0],payload=JSON.parse(fs.readFileSync(path.join(f.journal.directory,'objects',record.normalizedOutputDigest+'.json')));assert.match(payload.tests[0].durable_test_id,/^test:[a-f0-9]{64}$/);assert.equal(payload.tests[0].caseId,'semantic');assertDurable(f,s);});
 test('e2e A06 output lost before ingest recovers durable bytes from retained output on resume',async t=>{
