# Finding Verdicts

1. **Mutable state/maxima reopen recorded failed work — NOT ADDRESSED.** `scripts/verify-feature.sh:186–190` now rejects positive `review_rounds`, restores blocked, and preserves maxima/receipts, which closes the original positive-round probes under the amended conservative contract. However, `scripts/verify-feature.sh:130–151,179–201` accepts a ledger containing `review_rounds: 0` and positive blocker counts, and allows execution when those counts are below the configured ceiling. The controller-requested isolated probe with one recorded blocker and maximum two executed its marker, returned 0, and promoted F to passing. That is recorded failed work, not a clean first attempt. Treat any recorded blocker as recovery-requiring history, or reject the inconsistent ledger before effects. This fix needs no H04/H06 system and makes no claim about wholesale history replacement.

2. **Partial/null ledgers execute without recording attempts — ADDRESSED.** `scripts/verify-feature.sh:130–139` requires both ledger keys in any explicit non-null mapping before command emission; equivalent checks cover claims head and authority at `scripts/verify-claims.sh:93–101,223–230`. Empty, partial, and null mappings can no longer reach the previous writer failure. `tests/h01-hardening-regressions.py:110–135` covers both feature and claims input; the named methods are green in the inspected focal log.

3. **NUL commands execute altered bytes — ADDRESSED.** `scripts/verify-feature.sh:53–55,115–117`, `scripts/verify-claims.sh:45–47,79–81`, and `scripts/check-arch.sh:46–48,68–77` reject NUL before emission. Feature field reads now preserve trailing newlines using the sentinel at `scripts/verify-feature.sh:231–233`. The three permanent NUL marker tests are green in the inspected focal log.

4. **Decision slug collisions discard protected entries — ADDRESSED.** `scripts/verify-decisions.sh:77–102` reads bytes directly and requires the complete baseline as a prefix, eliminating slug naming, entry overwrites, CRLF normalization, and syntax-blind baseline trimming. Both collision and newline-byte tests are green in the inspected focal log. A separate append-boundary defect introduced by the replacement is listed below.

5. **A grep substring masks command failure — ADDRESSED.** `scripts/check-arch.sh:177–178` rejects every nonzero legacy-shell status. `scripts/check-arch.sh:72–91,169–176` uses an explicit argv/exit contract for matcher checks, and the root/full-template architecture configurations were migrated together. The inspected `test_arch_rule_requires_explicit_exit_contract` result covers the original `false # grep`, a valid grep no-match, and a missing executable. The new filter validation defect below is separate from the repaired substring inference.

6. **Duplicate JSON keys hide conflicting declarations — ADDRESSED.** Duplicate-detecting object hooks are installed in feature preflight and writers (`scripts/verify-feature.sh:45–51,79,253–260,329–336`), claims head/authority (`scripts/verify-claims.sh:38–43,55,169–178`), and architecture input (`scripts/check-arch.sh:35–40,52`). Permanent feature-depth, claims-head, claims-authority, and architecture cases are green in the inspected focal log.

# New Breakage in the Fix Diff

- **High/Important — Invalid regex filters can return PASS and execute effects.** `scripts/check-arch.sh:83–89` validates only filter shape/text; compilation occurs at `scripts/check-arch.sh:155` after `subprocess.run` at line 140. The no-match branch at lines 146–148 bypasses compilation altogether. An isolated typed matcher declared match=0/no-match=1, ran `sh -c 'touch executed; exit 1'`, and supplied invalid include pattern `(`. It created the marker, returned 0, and printed `[OK] R`. This new typed contract violates the H01 rule that invalid configuration must reject before any command effects. Compile every configured filter during whole-document preflight, before any rule executes, and reject invalid syntax with the configuration status. Retain a marker regression for a no-match result and an invalid filter; validate all rules before effects, not only the current rule.

- **High/Important — A suffix heading can extend the previous decision's final line.** `scripts/verify-decisions.sh:104–115` searches for a level-two heading relative to the suffix alone, without checking the actual document boundary. With baseline bytes `## A\nprotected original` (no final newline), a head ending `protected original## B\nappended text\n` passes: `## B` is at suffix offset zero but is not a document heading. The isolated probe returned 0 and `1 decision(s) intact`, although it appended text directly to the protected decision instead of a separate decision. Require the new heading to start on a real document line with a valid separator from the baseline's final content; retain exact baseline bytes. Add no-final-newline positive/negative append fixtures before repair.

# Out-of-Scope Observations

- None. Full-history replacement resistance remains the acknowledged external-witness H04 boundary; future recovery grants remain H06. This re-review does not re-open either milestone or recertify historical F09 behavior.

# Checks and Evidence

- Read the amended task brief and appended round-1 implementer report, then reviewed only the fix package from frozen index `0ca1ec165e699b155ed4498e6743cf8f4191874b` to `f1fa81c07241f1759203d2890e43c84520cc3484`. Diff sections were read in bounded groups; duplicate template hunks were checked for equality against their root hunks. All five mirror diffs were identical.
- Read `/workspace/scratch/adce1c53b293/h01-evidence/review-round1/focal-green-final.stderr.log` and `.exit`: 25 named tests, OK, exit 0. Read the final core summary, core stderr and exit: 283 passed, 0 failed, empty stderr, exit 0. Read complete quick-gate output, stderr and exit: 8 pass, 0 blocking, empty stderr, exit 0. Read `syntax-mirrors-final.log`: syntax 9/9 and mirrors 5/5. These are inspected stored results; no suite was rerun. Earlier RED runs remain implementer-reported evidence for this re-review.
- Ran exactly three isolated probes for the named unresolved doubts above. Inputs, copied verifier snapshots, exits, output, and marker observations are retained at `/workspace/scratch/adce1c53b293/h01-rereview1-probes-u43h26i_/`; result file: `/workspace/scratch/adce1c53b293/h01-rereview1-probes-u43h26i_/results.json`. All three reproduced the undesired success on the fix-round scripts. Repository, index, and HEAD remained read-only.
- The prior minor diagnostic finding is also addressed: `tests/run-tests.sh:58–63` retains focal output and prints it when the group fails.

# Verdict

**Fix round: Findings remain open.** Original finding 1 remains High/Important because positive blocker history can bypass the recovery restriction. Original findings 2–6 are addressed, but two new High/Important defects in filter preflight and decision append boundaries block approval. No Critical finding.
