# Independent H04 review

Reviewed base `96438e86b3d671027c8398b8c10edbb994e8ee59` against frozen tree `e3f83a2b15791072a195b8af75706479f2cbfcbc`, including the task brief, final H04 contract, runtime-boundary preflight, H03 API, shipping source/tests, oracle and provenance. Read-only review; no source, index or ref edits, no broad-suite rerun.

**Specification verdict: APPROVE with nonblocking Medium findings.** No supported-contract High/Critical defect found. Closed candidate operations expose negative run states and reservations only; opaque H03 context/approval verification remains authoritative. Final bindings are host-read and reclassified across exactly the originally accepted documents. Replacement preserves original acceptance, historical verdicts, lineage and objective spending. Witness-required replay fetches the host's latest data and verifies signed exact-head evidence; local replay is explicitly unwitnessed. Missing witness/target/recovery prerequisites stop. No effect, PASS, containment or actual external-service acceptance is claimed. H09 marker installation remains outside this change.

**Code-quality verdict: APPROVE with nonblocking Medium findings.** The small integration uses existing dependencies and the existing signature verifier. Exclusive locking, durable content/log ordering, CAS, strict replay, immutable operation inputs, conservative crash recovery and exact legacy-byte retention are coherent. The following narrow availability defects merit backlog fixes; neither grants authority, resets budget, produces PASS or authorizes an effect.

1. **Medium — valid long intent keys cannot reconcile.** `packs/autonomy/repo-template/scripts/quality-orchestrator/journal.mjs:7–17, 124, 218`. Candidate reservation keys accept 200 characters, while the outcome key also uses the 200-character `id` schema. `reconcile` prepends the ten-character `reconcile:` prefix. Thus an accepted 191–200-character reservation cannot append its outcome even when the host reports COMPLETED or NOT_APPLIED: parsing returns INCOMPLETE and its pending intent indefinitely blocks the next reservation/fresh run. Smallest fix: allow the internally constructed outcome key up to 210 characters while retaining the candidate bound and reserved-prefix restriction; add one maximum-length reservation/reconciliation regression case. This is directly established by schema bounds and the constructor path; no execution probe was run.

2. **Medium — publishing after a valid intervening witness can strand the retained floor.** `packs/autonomy/repo-template/scripts/quality-orchestrator/journal.mjs:156, 200–209`. With retained floor A, authenticated latest B linked to A, and the local journal already beyond B, `publishCheckpoint` successfully verifies B but does not advance the floor. It then publishes C linked to B and calls `verifiedWitness(C)`, which still requires C to link directly to A. C is rejected after the external CAS succeeds; subsequent latest reads return C and also fail against A. This can occur after an uncertain earlier publication followed by a further permitted local append. Unlike the documented missing-intermediate limitation, B was actually fetched and verified in this call. Smallest fix: retain the already authenticated, locally matched prior checkpoint B under ownership before publishing its successor, or validate the successor against that explicitly verified predecessor while updating the floor safely. Add one A/B/C regression case. Static control-flow finding; no execution probe was run.

Evidence inspection confirmed all 13 source-manifest and 35 evidence-manifest hashes, and all source-manifest bytes match the frozen tree. The archived implementer report hash is `a1e42c37a1dfcf77636b234862a073ad8b953a6abe559c20046c0fa451e413b9`. Recorded final static exit 0, runtime 29/29 and distinct public e2e 6/6 agree with the commands and logs. The single preserved defect snapshot contains the exact current test bytes; its causal log records two assertion failures (stale-run reservation and reserved-prefix collision), exit 1, separately from absent-API development evidence. These are inspected local receipts, not independent external authentication. Root owns F18 and full compatibility verification.

Neither finding requires a new framework, service, dependency, or expansion into H05–H09. Under the owner's explicit High/Critical-only blocking rule, H04 may proceed with these Medium items recorded.
